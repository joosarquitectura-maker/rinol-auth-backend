[build]
  command = "npm install"
  publish = "."

[functions]
  directory = "netlify/functions"

# Opcional: redirigir /api/* a las funciones
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/auth/:splat"
  status = 200
